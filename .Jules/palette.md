## 2024-10-18 - Theme Control Availability
**Learning:** Users visiting public-facing pages (like Login) were locked into the default theme, creating a jarring transition if their preference differed from system default.
**Action:** Ensure global preferences like Theme are available on all layout shells, including `(auth)` or `(admin)` public routes.
